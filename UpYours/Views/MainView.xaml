<Window x:Class="UpYours.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UpYours.Views"
        mc:Ignorable="d"
        xmlns:models="clr-namespace:UpYours.Models"
        xmlns:views="clr-namespace:UpYours.Views"
        Title="MainView" Height="300" Width="300">
    <Window.Resources>
        <ResourceDictionary>
            <DataTemplate  DataType="{x:Type models:Video}">
                <views:VideoView />
            </DataTemplate>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <ListBox x:Name="lbVideos" ItemsSource="{Binding Videos}" Margin="10,35,10,10" HorizontalContentAlignment="Stretch"/>
        <Button x:Name="Add" Content="Add" HorizontalAlignment="Left" Margin="432,10,0,0" VerticalAlignment="Top" Width="75"/>

    </Grid>
</Window>
